import { RoyaltySchedModel } from './royalty-sched-model';

describe('RoyaltySchedModel', () => {
  it('should create an instance', () => {
    expect(new RoyaltySchedModel()).toBeTruthy();
  });
});
